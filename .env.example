# Database URL - use your Neon Postgres connection string
# Get it from Neon dashboard: Prisma or .env.local tab (POSTGRES_PRISMA_URL)
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Owner dashboard - set a secret key to protect /owner (optional)
# If set, you must visit /owner?key=YOUR_SECRET or enter the key on /owner
# FLUX_OWNER_KEY=your-secret-key-here

# Owner email - used as reply-to for invite emails
FLUX_OWNER_EMAIL=shopifydevelopment0@gmail.com

# Resend - for sending invite emails (get API key from resend.com)
# Without this, invite links are generated but emails won't be sent
# RESEND_API_KEY=re_xxxxx
